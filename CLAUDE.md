# CLAUDE.md

## 执行守则

### 网络与代理

- **所有外网命令（git push、wrangler、npm install 等）必须走代理**：
  ```
  HTTP_PROXY=http://127.0.0.1:7897 HTTPS_PROXY=http://127.0.0.1:7897 <命令>
  ```
- 不用代理时外网必超时，直接带上，别再试一遍再想起

### Git 提交规范

- **永远不要加入**：`Co-Authored-By: Claude` 或任何类似的 AI 署名到 commit message 中
- 提交信息只写实际参与的人类作者


代码质量标准

所有脚本必须实施结构化的错误处理，具有特定的故障模式。

每个函数必须包含简洁的、目的明确的文档字符串。

脚本必须在执行关键或不可逆操作之前验证前置条件。

长时间运行的操作必须实施超时和取消机制。

文件和路径操作在授予访问权限之前必须验证存在性和权限。

⸻

文档协议

文档必须与代码变更同步—不应有过时的引用。

Markdown 文件必须使用一致的标题层级和节格式。

文档中的代码片段必须是可执行的、经过测试的，并反映真实的用例。

每个文档必须清楚地概述：目的、用法、参数和示例。

技术术语必须内联解释或链接到一个规范定义。

⸻

任务管理规则

任务必须清晰、具体并可操作—避免模糊。

每个任务必须指定一个负责的代理，并明确标记。

复杂的任务必须拆分成原子化、可追踪的子任务。

任何任务不得与现有的经过验证的系统行为相冲突或绕过。

与安全相关的任务必须经过指定审核者的强制审核。

代理必须在共享任务文件中更新任务状态和结果。

任务之间的依赖关系必须明确声明。

代理必须将模糊、矛盾或未界定的任务上报以寻求明确。

⸻

安全合规指南

禁止硬编码凭证—使用安全存储机制。

所有输入在处理之前必须经过验证、清理和类型检查。

避免使用eval、未经清理的shell调用或任何形式的命令注入向量。

文件和进程操作必须遵循最小权限原则。

所有敏感操作必须记录，但敏感数据值除外。

代理在访问受保护的服务或路径之前必须检查系统级权限。

⸻

流程执行要求

代理必须记录所有操作，具备适当的严重性（信息、警告、错误等）。

任何失败的任务必须包含清晰、可读的错误报告。

代理必须遵循系统资源限制，特别是内存和CPU的使用。

长时间运行的任务必须提供进度指示器或检查点。

重试逻辑必须包含指数退避和失败限制。

⸻

核心操作原则

代理不得在生产任务中使用模拟、回退或合成数据。

错误处理逻辑必须按照测试驱动原则设计。

代理必须始终根据可验证的证据行动，而不是假设。

所有前置条件在任何破坏性或高影响操作之前必须明确验证。

所有决策必须可以追溯到日志、数据或配置文件。

⸻

设计哲学原则

KISS（保持简单，愚蠢）
• 解决方案必须简单明了。
• 避免过度工程或不必要的抽象。
• 优先考虑代码的可读性和可维护性。

YAGNI（你不会需要它）
• 除非明确需要，否则不要添加推测的特性或未来保障。
• 只关注当前需求和可交付成果。
• 最小化代码膨胀和长期技术债务。

SOLID原则

单一职责原则—每个模块或函数应该只做一件事。

开闭原则—软件实体应该对扩展开放，但对修改关闭。

里氏替换原则—派生类必须可以替代其基类。

接口分离原则—优先使用多个具体接口，而非一个通用接口。

依赖倒置原则—依赖抽象，而不是具体实现。

⸻

系统扩展指南

所有新代理必须符合现有接口、日志和任务结构。

工具函数必须在共享使用之前经过单元测试和同行审查。

所有配置变更必须在系统清单中反映并带有版本印章。

新特性必须保持向后兼容，除非有正当理由并文档化。

所有更改必须包括性能影响评估。

⸻

质量保证程序

审核代理必须审核所有涉及安全、系统配置或代理角色的更改。

文档必须经过校对，以确保清晰性、一致性和技术正确性。

面向用户的输出（日志、消息、错误）必须清晰、非技术性和可操作。

所有错误信息应建议补救路径或诊断步骤。

所有重大更新必须包括回滚计划或安全还原机制。

⸻

测试与模拟规则

所有新逻辑必须包括单元测试和集成测试。

模拟或测试数据必须明确标记，并绝不能提升到生产。

所有测试在部署之前必须通过持续集成管道。

代码覆盖率应超过定义的阈值（例如85%）。

回归测试必须为所有高影响更新进行定义和执行。

代理必须在单独的测试日志中记录测试结果，而不是生产日志。

⸻

变更跟踪与治理

所有配置或规则更改必须在系统清单和变更日志中记录。

代理在修改共享资产时必须记录来源、时间戳和理由。

所有更新必须在适用时递增内部系统版本。

每次重大更改都必须定义回滚或撤销计划。

所有任务修改操作必须保留审计记录。
